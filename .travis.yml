dist: trusty
language: cpp
compiler:
  - gcc

before_install:
- sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
- sudo apt-get update -qq
- sudo apt-get install -qq qt5-default dpkg-buildpackage

script:
  - dpkg-buildpackage -rfakeroot
